spring:
  cloud:
    gcp:
      metrics:
        enabled: true
      core:
        enabled: true


management:
  metrics:
    export:
      stackdriver:
        enabled: true

logging:
  level:
    root: info
